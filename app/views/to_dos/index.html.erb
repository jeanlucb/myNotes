<h1>To-Do's</h1>


<%= paginate @to_dos, :theme => 'twitter-bootstrap-3',
    :pagination_class => "pagination pagination-sm" %>


<div class="container">

	<% @to_dos.each do |to_do| %>
		<%= render 'to_do_summary', to_do: to_do %>
	<% end %>


</div>

<br>

<%= link_to 'New To Do', new_to_do_path %>
